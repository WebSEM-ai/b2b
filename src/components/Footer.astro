---
interface Props {
    companyName?: string;
    email?: string;
    phone?: string;
    address?: string;
    links?: Array<{
        text: string;
        href: string;
    }>;
}

const {
    companyName = 'AlphaGrade by WebSEM',
    email = 'office@websem.ro',
    phone = '0760.60.48.49',
    address = 'B-dul Unirii 25-27, București',
    links = [
        { text: 'Disclaimer', href: '/disclaimer' },
        { text: 'Termeni și condiții', href: '/termeni' },
        { text: 'Politica de cookies', href: '/cookies' },
        { text: 'Politica de confidențialitate', href: '/confidentialitate' }
    ]
} = Astro.props;

const year = new Date().getFullYear();
---

<!-- Footer CTA Section -->
<section class="footer-cta-section" id="contact">
    <div class="footer-cta-bg">
        <span class="footer-alpha-bg">α</span>
        <div class="footer-cta-glow"></div>
    </div>

    <div class="footer-cta-container">
        <div class="footer-cta-content">
            <span class="footer-cta-badge">Începe Acum</span>
            <h2 class="footer-cta-title">
                Gata să-ți <span class="gradient-text">Transformi</span> Pipeline-ul B2B?
            </h2>
            <p class="footer-cta-subtitle">
                Primii 10 clienți din această lună beneficiază de <strong>audit gratuit</strong> și <strong>50 lead-uri bonus</strong>.
            </p>

            <div class="footer-cta-stats">
                <div class="footer-stat-item">
                    <span class="footer-stat-value">265M+</span>
                    <span class="footer-stat-label">Contacte Verificate</span>
                </div>
                <div class="footer-stat-divider"></div>
                <div class="footer-stat-item">
                    <span class="footer-stat-value">98%</span>
                    <span class="footer-stat-label">Acuratețe Email</span>
                </div>
                <div class="footer-stat-divider"></div>
                <div class="footer-stat-item">
                    <span class="footer-stat-value">72h</span>
                    <span class="footer-stat-label">Setup Complet</span>
                </div>
            </div>
        </div>

        <div class="footer-cta-form-wrapper">
            <form class="footer-cta-form" id="footerCtaForm">
                <div class="footer-form-row">
                    <div class="footer-form-field">
                        <input type="url" name="website" placeholder="Website-ul tău" required>
                        <span class="field-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="10"/>
                                <line x1="2" y1="12" x2="22" y2="12"/>
                                <path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/>
                            </svg>
                        </span>
                    </div>
                    <div class="footer-form-field">
                        <input type="tel" name="phone" placeholder="Telefon" required>
                        <span class="field-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                            </svg>
                        </span>
                    </div>
                </div>
                <div class="footer-form-row">
                    <div class="footer-form-field footer-form-field-full">
                        <input type="email" name="email" placeholder="Email" required>
                        <span class="field-icon">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
                                <polyline points="22,6 12,13 2,6"/>
                            </svg>
                        </span>
                    </div>
                </div>
                <button type="submit" class="footer-form-submit">
                    <span class="btn-text">Solicită Audit Gratuit</span>
                    <span class="btn-icon">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
                            <path d="M5 12h14M12 5l7 7-7 7"/>
                        </svg>
                    </span>
                    <span class="btn-shine"></span>
                </button>
            </form>
            <div class="footer-form-success" id="footerFormSuccess">
                <div class="success-icon">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/>
                        <polyline points="22 4 12 14.01 9 11.01"/>
                    </svg>
                </div>
                <div class="success-content">
                    <h4>Excelent!</h4>
                    <p>Te contactăm în maximum 24h cu analiza completă.</p>
                </div>
            </div>
            <p class="footer-form-disclaimer">
                * Datele de contact sunt furnizate prin <strong>Apollo.io</strong>, <strong>LinkedIn Sales Navigator</strong> și alte platforme B2B outreach. Nu deținem aceste baze de date - oferim servicii de prospectare și outreach.
            </p>
        </div>
    </div>
</section>

<footer>
    <div class="footer-container">
        <div class="footer-main">
            <div class="footer-brand">
                <a href="/" class="footer-logo">
                    <span class="logo-symbol">α</span>
                    <span class="logo-text">
                        <span class="logo-alpha">ALPHA</span><span class="logo-sep">/</span><span class="logo-grade">GRADE</span>
                    </span>
                </a>
                <p class="footer-tagline">B2B Sales Intelligence by WebSEM</p>
            </div>

            <div class="footer-contact">
                <div class="contact-item">
                    <span class="contact-label">Email:</span>
                    <a href={`mailto:${email}`}>{email}</a>
                </div>
                <div class="contact-item">
                    <span class="contact-label">Telefon:</span>
                    <a href={`tel:${phone.replace(/\D/g, '')}`}>{phone}</a>
                </div>
                <div class="contact-item">
                    <span class="contact-label">Adresă:</span>
                    <span>{address}</span>
                </div>
            </div>
        </div>

        <div class="footer-bottom">
            <p class="copyright">© {year} {companyName}. Toate drepturile rezervate.</p>
            <div class="footer-links">
                {links.map(link => (
                    <a href={link.href}>{link.text}</a>
                ))}
            </div>
        </div>
    </div>
</footer>

<script>
    // Footer form submission
    const footerForm = document.getElementById('footerCtaForm') as HTMLFormElement;
    const footerSuccess = document.getElementById('footerFormSuccess');
    const footerSubmitBtn = footerForm?.querySelector('button[type="submit"]') as HTMLButtonElement;
    const originalFooterBtnText = footerSubmitBtn?.innerHTML;

    footerForm?.addEventListener('submit', async (e) => {
        e.preventDefault();

        if (footerSubmitBtn) {
            footerSubmitBtn.disabled = true;
            footerSubmitBtn.innerHTML = `
                <svg class="spinner" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" style="width:20px;height:20px;animation:spin 1s linear infinite">
                    <circle cx="12" cy="12" r="10" stroke-opacity="0.25"/>
                    <path d="M12 2a10 10 0 0 1 10 10" stroke-opacity="1"/>
                </svg>
                Se trimite...
            `;
        }

        const formData = new FormData(footerForm);
        const data = {
            email: formData.get('email'),
            phone: formData.get('phone'),
            website: formData.get('website')
        };

        try {
            const response = await fetch('/api/pipedrive', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });

            const result = await response.json();

            if (response.ok && result.success) {
                footerForm.classList.add('hidden');
                footerSuccess?.classList.add('visible');
            } else {
                throw new Error(result.error || 'Eroare la trimitere');
            }
        } catch (error) {
            if (footerSubmitBtn) {
                footerSubmitBtn.disabled = false;
                footerSubmitBtn.innerHTML = originalFooterBtnText;
            }
            alert('A apărut o eroare. Te rugăm să încerci din nou.');
            console.error('Footer form error:', error);
        }
    });
</script>

<style>
    /* Footer CTA Section */
    .footer-cta-section {
        position: relative;
        padding: 5rem 2rem;
        background: linear-gradient(180deg, var(--bg-primary) 0%, var(--bg-secondary) 50%, var(--bg-tertiary) 100%);
        overflow: hidden;
    }

    .footer-cta-bg {
        position: absolute;
        inset: 0;
        pointer-events: none;
    }

    .footer-alpha-bg {
        position: absolute;
        font-family: 'Times New Roman', serif;
        font-size: 50rem;
        font-style: italic;
        color: var(--alpha-bg);
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        animation: floatAlpha 20s ease-in-out infinite;
    }

    @keyframes floatAlpha {
        0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.5; }
        50% { transform: translate(-50%, -52%) scale(1.02); opacity: 0.8; }
    }

    .footer-cta-glow {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 600px;
        height: 600px;
        background: radial-gradient(circle, rgba(99, 102, 241, 0.15) 0%, transparent 70%);
        animation: pulseGlow 4s ease-in-out infinite;
    }

    @keyframes pulseGlow {
        0%, 100% { opacity: 0.5; transform: translate(-50%, -50%) scale(1); }
        50% { opacity: 0.8; transform: translate(-50%, -50%) scale(1.1); }
    }

    .footer-cta-container {
        max-width: 1100px;
        margin: 0 auto;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 4rem;
        align-items: center;
        position: relative;
        z-index: 1;
    }

    .footer-cta-badge {
        display: inline-block;
        padding: 0.4rem 1rem;
        background: rgba(99, 102, 241, 0.15);
        border: 1px solid rgba(99, 102, 241, 0.3);
        border-radius: 100px;
        font-size: 0.75rem;
        font-weight: 600;
        letter-spacing: 0.1em;
        text-transform: uppercase;
        color: var(--accent-light);
        margin-bottom: 1.5rem;
    }

    .footer-cta-title {
        font-family: 'Space Grotesk', sans-serif;
        font-size: 2.5rem;
        font-weight: 700;
        line-height: 1.2;
        margin-bottom: 1rem;
        color: var(--text-primary);
    }

    .gradient-text {
        background: linear-gradient(135deg, #667eea, #764ba2, #f093fb);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .footer-cta-subtitle {
        font-size: 1.1rem;
        color: var(--text-secondary);
        line-height: 1.6;
        margin-bottom: 2rem;
    }

    .footer-cta-subtitle strong {
        color: var(--text-primary);
    }

    .footer-cta-stats {
        display: flex;
        align-items: center;
        gap: 1.5rem;
    }

    .footer-stat-item {
        display: flex;
        flex-direction: column;
    }

    .footer-stat-value {
        font-family: 'Space Grotesk', sans-serif;
        font-size: 1.5rem;
        font-weight: 700;
        background: linear-gradient(135deg, #667eea, #a78bfa);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .footer-stat-label {
        font-size: 0.7rem;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }

    .footer-stat-divider {
        width: 1px;
        height: 40px;
        background: var(--border-light);
    }

    /* Footer Form */
    .footer-cta-form-wrapper {
        background: var(--bg-card);
        border: 1px solid var(--border-light);
        border-radius: 24px;
        padding: 2rem;
        position: relative;
    }

    .footer-cta-form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .footer-cta-form.hidden {
        display: none;
    }

    .footer-form-row {
        display: flex;
        gap: 1rem;
    }

    .footer-form-field {
        flex: 1;
        position: relative;
    }

    .footer-form-field-full {
        flex: 1;
    }

    .footer-form-field input {
        width: 100%;
        padding: 1rem 1.25rem;
        padding-right: 3rem;
        background: var(--bg-input);
        border: 1px solid var(--border-medium);
        border-radius: 12px;
        color: var(--text-primary);
        font-size: 0.95rem;
        font-family: 'Inter', sans-serif;
        transition: all 0.3s ease;
    }

    .footer-form-field input::placeholder {
        color: var(--text-muted);
    }

    .footer-form-field input:focus {
        outline: none;
        border-color: var(--border-accent);
        background: var(--bg-input-focus);
        box-shadow: var(--shadow-glow);
    }

    .field-icon {
        position: absolute;
        right: 1rem;
        top: 50%;
        transform: translateY(-50%);
        color: var(--text-muted);
        pointer-events: none;
    }

    .field-icon svg {
        width: 18px;
        height: 18px;
    }

    .footer-form-submit {
        margin-top: 0.5rem;
        padding: 1.15rem 2rem;
        background: linear-gradient(135deg, #667eea, #764ba2);
        border: none;
        border-radius: 12px;
        color: var(--white);
        font-size: 1rem;
        font-weight: 600;
        font-family: 'Inter', sans-serif;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.75rem;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }

    .footer-form-submit:hover {
        transform: translateY(-2px);
        box-shadow: 0 15px 40px rgba(99, 102, 241, 0.4);
    }

    .footer-form-submit:disabled {
        opacity: 0.7;
        cursor: not-allowed;
        transform: none;
    }

    .btn-icon svg {
        width: 18px;
        height: 18px;
        transition: transform 0.3s ease;
    }

    .footer-form-submit:hover:not(:disabled) .btn-icon svg {
        transform: translateX(4px);
    }

    .btn-shine {
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
        animation: shine 3s ease-in-out infinite;
    }

    @keyframes shine {
        0%, 100% { left: -100%; }
        50% { left: 100%; }
    }

    .footer-form-success {
        display: none;
        align-items: center;
        gap: 1rem;
        padding: 2rem;
    }

    .footer-form-success.visible {
        display: flex;
    }

    .footer-form-success .success-icon {
        width: 60px;
        height: 60px;
        background: rgba(34, 197, 94, 0.15);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
    }

    .footer-form-success .success-icon svg {
        width: 30px;
        height: 30px;
        color: var(--green);
    }

    .footer-form-success h4 {
        font-family: 'Space Grotesk', sans-serif;
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--green);
        margin-bottom: 0.25rem;
    }

    .footer-form-success p {
        color: var(--text-secondary);
        font-size: 0.9rem;
    }

    .footer-form-disclaimer {
        margin-top: 1rem;
        font-size: 0.7rem;
        color: var(--text-muted);
        line-height: 1.5;
        text-align: center;
    }

    .footer-form-disclaimer strong {
        color: var(--text-tertiary);
    }

    /* Main Footer */
    footer {
        background: var(--bg-secondary);
        border-top: 1px solid var(--border-light);
        padding: 3rem 2rem 2rem;
    }

    .footer-container {
        max-width: 1400px;
        margin: 0 auto;
    }

    .footer-main {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 3rem;
        padding-bottom: 2rem;
        border-bottom: 1px solid var(--border-light);
        margin-bottom: 2rem;
    }

    .footer-logo {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
    }

    .footer-logo .logo-symbol {
        font-family: 'Times New Roman', serif;
        font-size: 1.5rem;
        font-style: italic;
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .footer-logo .logo-text {
        font-family: 'Orbitron', 'Space Grotesk', sans-serif;
        font-size: 0.9rem;
        font-weight: 700;
        letter-spacing: 0.05em;
    }

    .footer-logo .logo-alpha {
        color: var(--text-primary);
    }

    .footer-logo .logo-sep {
        color: var(--text-muted);
    }

    .footer-logo .logo-grade {
        background: linear-gradient(135deg, #667eea, #764ba2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .footer-tagline {
        margin-top: 0.5rem;
        font-size: 0.8rem;
        color: var(--text-muted);
    }

    .footer-contact {
        display: flex;
        gap: 2rem;
    }

    .contact-item {
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
    }

    .contact-label {
        font-size: 0.7rem;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--text-muted);
    }

    .contact-item a,
    .contact-item span:not(.contact-label) {
        color: var(--text-secondary);
        text-decoration: none;
        font-size: 0.9rem;
        transition: color 0.3s ease;
    }

    .contact-item a:hover {
        color: var(--accent-light);
    }

    .footer-bottom {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .copyright {
        font-size: 0.8rem;
        color: var(--text-muted);
    }

    .footer-links {
        display: flex;
        gap: 1.5rem;
    }

    .footer-links a {
        color: var(--text-muted);
        text-decoration: none;
        font-size: 0.8rem;
        transition: color 0.3s ease;
    }

    .footer-links a:hover {
        color: var(--text-primary);
    }

    /* Mobile Styles */
    @media (max-width: 900px) {
        .footer-cta-container {
            grid-template-columns: 1fr;
            gap: 2rem;
            text-align: center;
        }

        .footer-cta-title {
            font-size: 1.75rem;
        }

        .footer-cta-stats {
            justify-content: center;
        }

        .footer-form-row {
            flex-direction: column;
        }

        .footer-main {
            flex-direction: column;
            gap: 2rem;
            text-align: center;
            align-items: center;
        }

        .footer-brand {
            text-align: center;
        }

        .footer-logo {
            justify-content: center;
        }

        .footer-contact {
            flex-direction: column;
            gap: 1rem;
            align-items: center;
        }

        .contact-item {
            align-items: center;
        }

        .footer-bottom {
            flex-direction: column;
            gap: 1rem;
            text-align: center;
        }

        .footer-links {
            flex-wrap: wrap;
            justify-content: center;
            gap: 1rem;
        }
    }

    @media (max-width: 480px) {
        .footer-cta-stats {
            flex-direction: column;
            gap: 1rem;
        }

        .footer-stat-divider {
            width: 40px;
            height: 1px;
        }
    }
</style>
