---
interface Props {
    title?: string;
    logos?: Array<{
        src: string;
        alt: string;
    }>;
}

const {
    title = 'Companii care folosesc AlphaGrade',
    logos = [
        { src: '/LOGOS/logo-brd.png', alt: 'BRD' },
        { src: '/LOGOS/CATENA.png', alt: 'Catena' },
        { src: '/LOGOS/engie-300x164-3.png', alt: 'Engie' },
        { src: '/LOGOS/MOL.png', alt: 'MOL' },
        { src: '/LOGOS/geberit-300x164-3.png', alt: 'Geberit' },
        { src: '/LOGOS/logo-christian-tour.png', alt: 'Christian Tour' },
        { src: '/LOGOS/farmaciadona-300x164-3.png', alt: 'Farmacia Dona' },
        { src: '/LOGOS/e-egger-300x164-1-2.png', alt: 'Egger' },
        { src: '/LOGOS/yves-roucher.png', alt: 'Yves Rocher' }
    ]
} = Astro.props;

// Duplicate logos for seamless loop
const allLogos = [...logos, ...logos];
---

<section class="clients-section">
    <div class="clients-header">
        <h3>{title}</h3>
    </div>

    <div class="logo-ticker-wrapper">
        <div class="logo-ticker" id="logoTicker">
            {allLogos.map(logo => (
                <div class="logo-item">
                    {logo.src ? (
                        <img src={logo.src} alt={logo.alt} loading="lazy" />
                    ) : (
                        <span class="logo-placeholder">{logo.alt}</span>
                    )}
                </div>
            ))}
        </div>
    </div>
</section>
